# Construction Material Prices — Synthetic Data

[![CI](https://github.com/Pranit-satnurkar/Construction-Price-Tracker/actions/workflows/ci.yml/badge.svg)](https://github.com/Pranit-satnurkar/Construction-Price-Tracker/actions)

This repository contains a small utility to synthesize daily construction material
prices for testing and demonstration.

Files:
- `generate_data.py`: Generates synthetic daily prices and writes a CSV.
- `construction_material_prices.csv`: (may be generated by the script)

Quick start
-----------

Install dependencies (recommended inside a virtual environment):

```pwsh
python -m pip install -r requirements.txt
```

Generate a CSV (PowerShell):

```pwsh
python .\generate_data.py --output construction_material_prices.csv
```

Options:
- `--output` / `-o`: Path to output CSV (default: `construction_material_prices.csv`).
- `--days` / `-d`: Number of days of history to generate (default: 365).
- `--seed`: Random seed for reproducibility (default: 42).

Development
-----------

Run tests with `pytest`:

```pwsh
python -m pytest -q
```

Repository status
-----------------

- `main` branch contains the current stable code.
- `refactor/generate-data` contains the refactor branch.
- CI workflow runs on pushes and pull requests.

Open pull requests:

- Merge `refactor/generate-data` into `main`:
  https://github.com/Pranit-satnurkar/Construction-Price-Tracker/pull/new/refactor/generate-data
- CI workflow PR:
  https://github.com/Pranit-satnurkar/Construction-Price-Tracker/pull/new/ci/add-gh-actions

If you create a GitHub repository for this folder, commit the code and exclude
local virtual environments and large data files via `.gitignore` (provided).
# Construction Material Prices — Synthetic Data

This repository contains a small utility to synthesize daily construction material
prices for testing and demonstration.

Files:
- `generate_data.py`: Generates synthetic daily prices and writes a CSV.
- `construction_material_prices.csv`: (may be generated by the script)

Usage:

PowerShell (Windows):
```pwsh
python .\generate_data.py --output construction_material_prices.csv
```

Options:
- `--output` / `-o`: Path to output CSV (default: `construction_material_prices.csv`).
- `--days` / `-d`: Number of days of history to generate (default: 365).
- `--seed`: Random seed for reproducibility (default: 42).

If you create a GitHub repository for this folder, commit the code and exclude
local virtual environments and large data files via `.gitignore` (provided).
